# GPT-5.6 Luna Max 构建任务书

## 项目名称

**一念成笺 · 国风诗签**

副标题：**把此刻心情，化成一张可收藏、可分享、可同题挑战的国风诗签。**

---

## 0. 执行指令

你是一名资深产品经理、前端工程师、中文内容设计师、国风视觉设计师和质量工程师。请直接在以下本地项目中完成构建，不要只给建议、伪代码或局部片段：

```text
D:\xhs\guofeng-vibecoding-studio
```

执行要求：

1. 先读取现有 `index.html`、`styles.css`、`app.js`、`gemini.js`、`README.md`、`docs/NOTE.md`、`docs/SUBMIT.md` 和 `scripts/selfcheck.js`。
2. 在现有项目上迭代，保留已经可用的诗签生成、Canvas 绘制、保存图片和复制文案能力。
3. 直接修改并补全所有文件，最终交付可运行的完整项目。
4. 不安装 npm 依赖，不引入框架，不依赖后端。
5. 不使用外部字体、图片、图标库或 CDN。
6. 基础体验在 `file://` 下可运行；通过静态服务器和小红书 WebView 运行时功能完整。
7. 所有生产资源使用相对路径。
8. 所有用户输入必须安全处理，不得直接拼入不可信 `innerHTML`。
9. 完成后必须运行自检、语法检查、桌面端和移动端验收。
10. 完成后提交 Git，提交信息使用：

```text
feat: rebuild yinian-chengjian for xhs contests
```

---

## 1. 比赛目标

本项目需要使用同一作品同时参加两项活动：

### 活动 A：国风 Vibecoding 共创

必须满足：

- 上传为小红书小工具；
- 在参赛笔记中成功挂载小工具；
- 使用话题 `#国风vibecoding`；
- 正文加入 `@科技薯`；
- 如果内容涉及历史事实，展示真实、可核验的来源，不得编造史料。

### 活动 B：小红书 Vibecoding 大赛

必须满足：

- 上传为小红书小工具；
- 在参赛笔记中挂载工具；
- 使用话题 `#小红书vibecoding大赛`；
- 选择赛道话题 `#vibeart`；
- 产品定位应符合“将情绪和想象转化为可观看、可互动的数字艺术体验”。

### 最终统一参赛标签

```text
#国风vibecoding #小红书vibecoding大赛 #vibeart
#vibecoding #小红书小工具 #国风诗签
@科技薯
```

不要继续使用以下不准确或非必需标签：

```text
#vibecoding大赛
#vibecoding里的国风世界
#AI写诗
```

---

## 2. 产品定位

### 一句话价值

用户选择一种心绪和文化意象，工具生成一首结构完整的四句诗签、一段创作释义和一张 3:4 高清国风卡片，并支持保存、复制和发起同题挑战。

### 目标用户

- 喜欢国风、诗词、手账、壁纸和情绪表达的小红书用户；
- 想快速获得个性化分享素材的普通用户；
- 对传统文化数字化表达感兴趣的年轻用户。

### 核心差异

项目不是普通“随机诗句生成器”，而是一个具有以下闭环的互动艺术工具：

```text
表达心情
→ 选择文化意象
→ 生成四句诗签
→ 阅读创作释义
→ 保存3:4卡片
→ 邀请朋友生成同题签
```

---

## 3. 生产版清理

必须先完成以下清理：

1. 从 `index.html` 删除整个 `#geminiModal`。
2. 从 `index.html` 删除 `<script src="./gemini.js"></script>`。
3. 生产页面不得出现 API Key、模型选择、前端优化等开发者控件。
4. `gemini.js` 可从生产项目删除；如果保留作开发记录，也不得被页面加载或打入小红书上传包。
5. 删除与隐藏 Gemini 面板相关且不再使用的 CSS。
6. 更新 README 和参赛文档，移除“页面内置 Gemini 面板”的说明。
7. 生产包不得包含 `.git`、MCP 报告、登录脚本、备份文件、测试截图或 API 配置。

---

## 4. 信息架构

页面只保留五个区域：

### 4.1 紧凑首屏

- 品牌：一念成笺；
- 副标题：把此刻心情，化成一张国风诗签；
- 三步说明：选心绪 → 成诗签 → 存图分享；
- 移动端首屏高度应控制在约 220px 内，主要输入区域必须尽快进入视野。

### 4.2 创作区

包含：

- 九个心绪快捷按钮；
- 自定义心境关键词；
- 文化意象选择；
- 五言诗签／七言诗签选择；
- 用户落款；
- 生成按钮。

### 4.3 结果区

包含：

- 诗签标题；
- 四句诗文；
- 创作释义；
- 灵感来源；
- 3:4 Canvas 预览。

### 4.4 操作区

主次顺序必须为：

1. 保存诗签；
2. 换一签；
3. 复制发布文案；
4. 邀请朋友同题创作。

### 4.5 最近诗签

- 使用 `localStorage` 保存最近 12 张诗签的数据；
- 只保存文本参数和种子，不保存 Canvas Base64；
- 支持恢复、再次下载和清空；
- 首次使用时不显示空白历史容器。

---

## 5. 输入设计

### 5.1 心绪快捷项

保留九种心绪，但将其定义为“心绪”，不是简单单字替换：

```text
清欢、思念、孤勇、松弛、期许、释然、怦然、归心、远行
```

每个按钮必须：

- 有稳定 `data-mood`；
- 有明显选中样式；
- 使用 `aria-pressed="true|false"`；
- 点击后更新输入框，但不立即产生多次无意识随机结果。

### 5.2 自定义关键词

- 限制 1–4 个中文字符；
- 自动移除 HTML 标签、控制字符和多余空格；
- 超出长度时给出 Toast；
- 恶意输入只能作为纯文本显示。

### 5.3 文化意象

首版提供四类：

```text
山水｜节气｜雅器｜志怪
```

每类至少提供 4 个子主题：

```text
山水：松风、寒江、远岫、竹雨
节气：春分、谷雨、白露、霜降
雅器：古琴、团扇、香篆、砚墨
志怪：青鸟、烛龙、九尾、鲲鹏
```

规则：

- 文化主题用于控制全诗词库、色彩和创作释义；
- “创作释义”必须明确是艺术化解读，不冒充历史事实；
- 若添加历史背景，数据中必须包含 `sourceTitle` 与 `sourceUrl`；
- 没有核验来源时，不输出历史年份、人物经历或非遗工艺事实。

### 5.4 落款

- 提供可选昵称，限制 1–6 个中文字符；
- 未输入时使用“无名客”；
- 不再每次随机更换作者名；
- 落款进入 Canvas 和分享文案。

---

## 6. 诗签生成引擎

### 6.1 命名原则

在没有实现严格平仄校验前，界面必须使用：

```text
五言诗签
七言诗签
```

不要使用“平起、仄起、平水韵、绝句格律”等专业承诺。

### 6.2 固定结构

每次必须生成四句：

```text
第1句：起景
第2句：承情
第3句：转意
第4句：合境
```

字数要求：

- 五言：4句，每句严格5个中文字符；
- 七言：4句，每句严格7个中文字符；
- 标点不计入诗句字符；
- 页面展示时统一在句末添加中文标点；
- Canvas 中只绘制正文汉字，不绘制连续句号。

### 6.3 词库结构

为每个文化主题建立独立词库：

```javascript
{
  id: "THEME_ID",
  category: "CATEGORY_ID",
  name: "主题名",
  titlePool: [],
  scenes: [],
  motions: [],
  feelings: [],
  turns: [],
  endings: [],
  rhymeGroups: {
    an: [],
    ou: [],
    ing: []
  },
  interpretationTemplates: [],
  sourceTitle: "",
  sourceUrl: ""
}
```

### 6.4 连贯性约束

必须实现：

1. 一首诗只使用一个文化主题；
2. 第2句和第4句使用同一收束韵组；
3. 同一首诗不得重复完整词组；
4. 同一首诗不得出现两句完全相同；
5. 用户关键词应自然进入第1句或标题，不要粗暴替换首字；
6. 如果关键词长度破坏句长，优先放入标题；
7. 每次生成后执行 `validatePoem()`；
8. 校验失败最多重试 20 次，最终使用预置保底诗签；
9. 生成结果必须包含 `seed`，确保预览、下载和历史恢复完全一致。

### 6.5 随机策略

实现轻量、可复现的种子随机函数：

```text
seed = hash(关键词 + 心绪 + 主题 + 字数 + rerollIndex)
```

要求：

- 相同参数和相同 `rerollIndex` 得到相同结果；
- 点击“换一签”只增加 `rerollIndex`；
- Canvas 纸张纹理也使用同一随机种子，下载结果必须与预览一致。

### 6.6 输出数据结构

```javascript
{
  id: "唯一ID",
  seed: 0,
  mood: "清欢",
  keyword: "暮雪",
  themeId: "frost-descent",
  themeName: "霜降",
  form: 7,
  title: "暮雪成笺",
  lines: ["七字诗句", "七字诗句", "七字诗句", "七字诗句"],
  interpretation: "艺术化创作释义",
  author: "落款",
  sourceTitle: "",
  sourceUrl: "",
  createdAt: 0,
  rerollIndex: 0
}
```

---

## 7. Canvas 诗签规范

### 7.1 尺寸

使用真正的 3:4 比例：

```text
逻辑尺寸：600 × 800
DPR：2
实际导出：1200 × 1600 PNG
```

不要继续使用 `520×720`。

### 7.2 布局

- 四句正文采用竖排；
- 第一句位于最右侧，第四句位于最左侧；
- 标题位于右上或上方中部；
- 落款、日期与方形朱印位于左下；
- 主题小印位于右上；
- 所有文字必须位于安全边距内；
- 五言和七言分别计算字号、字距和起始高度；
- 任何 320px 宽的页面预览都不得裁切 Canvas。

### 7.3 三套本地视觉主题

```text
宣纸朱砂：米白、焦墨、朱砂、洒金
黛青月白：月白、黛青、银灰、暗金
竹青松烟：竹青、松烟、浅绢、赭石
```

所有纹理使用 Canvas 或 CSS 本地生成，不加载外部图片。

### 7.4 导出

- 文件名：`一念成笺-关键词-时间戳.png`；
- 导出前重新按同一种子绘制；
- 导出成功显示 Toast；
- 导出异常时提供“长按图片保存”提示；
- PNG 不得模糊、拉伸或比例错误。

---

## 8. 分享闭环

### 8.1 发布文案

生成以下结构，不使用“爆款文案”措辞：

```text
🍂 一念成笺｜我的今日国风诗签

我把「{keyword}」写进了「{themeName}」的意象里：

{line1}。
{line2}。
{line3}。
{line4}。

签语：{interpretation}
落款：{author}

也来输入你的心情，看看会得到哪一张诗签。

@科技薯
#国风vibecoding #小红书vibecoding大赛 #vibeart
#vibecoding #小红书小工具 #国风诗签
```

### 8.2 同题挑战

实现“邀请朋友同题创作”：

- 使用 URL Hash 保存 `mood`、`keyword`、`themeId` 和 `form`；
- 不在 URL 中保存用户落款；
- 页面加载时恢复挑战参数；
- 显示“来自朋友的同题邀请”；
- 用户点击生成时使用新的种子，得到同题不同签；
- 优先调用 `navigator.share()`；
- 不支持时复制挑战文案和链接；
- `file://` 环境没有可分享 URL 时，只复制同题挑战文案。

挑战文案：

```text
我用「{keyword}」抽到了一张「{themeName}」诗签，邀请你也来写同题签。
```

---

## 9. 交互与移动端

### 桌面端

- 最大内容宽度约 1100px；
- 左侧创作，右侧实时预览；
- 操作区层级明确，不超过两个主色按钮。

### 移动端

- `880px` 以下切换单列；
- 主要按钮高度不少于 44px；
- 不出现水平滚动；
- 生成完成后平滑滚动到结果卡；
- 支持 `env(safe-area-inset-bottom)`；
- 页面底部可使用轻量粘性操作栏，但不得遮挡内容；
- 在 320、375、390、430px 四种宽度验收。

### 状态反馈

- 生成成功；
- 保存成功；
- 文案复制成功；
- 挑战链接复制成功；
- 输入被截断；
- 历史清空；
- 所有反馈使用统一 Toast，不使用阻塞式 `alert()`。

### 动效

- 纸张展开、印章落下和诗句淡入均控制在 500ms 内；
- 不使用连续粒子动画；
- 支持 `prefers-reduced-motion: reduce`；
- 动效不影响点击与阅读。

---

## 10. 国风视觉系统

### 色彩变量

```css
--ink-900: #1a140e;
--ink-700: #2c2218;
--paper-100: #fbf7ee;
--paper-200: #f3e9d2;
--cinnabar: #a6341b;
--cinnabar-dark: #872712;
--gold: #9e7532;
--bamboo: #526b53;
--indigo: #26384a;
```

### 设计原则

- 中国审美优先，避免普通 SaaS 卡片感；
- 使用留白、细边框、朱砂印章、宣纸层次和竖排诗文；
- Emoji 只用于少量情绪提示，不作为主要视觉语言；
- 不加载在线字体；
- 字体栈使用系统楷体、宋体和 serif 回退；
- 所有正文对比度满足清晰阅读；
- 按钮必须有 `hover`、`active` 和 `focus-visible` 状态。

---

## 11. 安全与隐私

1. 不收集账号、手机号、位置或设备标识；
2. 不包含 API Key 输入框；
3. 不调用 Gemini 或其他生成式 API；
4. 不发送用户心情、落款和历史数据；
5. 所有历史记录只保存在当前浏览器 `localStorage`；
6. 用户输入使用 `textContent` 或严格转义；
7. URL Hash 不保存落款和历史；
8. 分享文案不得包含不可见控制字符；
9. 页面不得使用 `eval()` 或远程脚本；
10. Console 中不得记录用户完整输入。

---

## 12. 文件交付

必须完成以下文件：

```text
index.html
styles.css
app.js
README.md
docs/NOTE.md
docs/SUBMIT.md
scripts/selfcheck.js
scripts/package-xhs.ps1
assets/favicon.svg
```

允许新增：

```text
assets/
release/
```

不要新增框架配置、打包器或 `node_modules`。

### 生产上传包

新增 `scripts/package-xhs.ps1`，生成：

```text
release/yinian-chengjian-xhs.zip
```

压缩包根目录只允许包含：

```text
index.html
styles.css
app.js
assets/favicon.svg
```

生产包不得包含：

```text
.git/
docs/
scripts/
gemini.js
GEMINI_MCP_REVIEW.md
BUILD_SPEC_GPT56_LUNA_MAX.md
*.bak
任何 API Key 或日志
```

打包脚本必须先执行自检，自检失败时停止生成压缩包。

---

## 13. 自检要求

重写 `scripts/selfcheck.js`，对真实 `app.js` 逻辑进行测试，不要复制一份独立的旧算法。

必须测试：

1. 五言诗签始终为4句；
2. 五言每句严格5个中文字符；
3. 七言诗签始终为4句；
4. 七言每句严格7个中文字符；
5. 连续生成至少100首，无重复完整诗句；
6. 同一首内没有重复句；
7. 第2句和第4句属于同一配置韵组；
8. 相同种子得到相同诗签；
9. 不同 `rerollIndex` 能得到不同结果；
10. 恶意输入不会生成 HTML 节点；
11. Canvas 实际尺寸为1200×1600；
12. Canvas 第一句位于最右侧；
13. 参赛文案包含三个必需项：

```text
#国风vibecoding
#小红书vibecoding大赛
#vibeart
```

14. 页面不存在 `#geminiModal`；
15. 页面不加载 `gemini.js`；
16. 页面不存在外部字体、图片、脚本或 CDN；
17. 所有必需 DOM ID 存在；
18. 历史记录最多12条；
19. URL Hash 恢复不包含用户落款；
20. `prefers-reduced-motion` 样式存在。

运行命令：

```powershell
node --check .\app.js
node --check .\scripts\selfcheck.js
node .\scripts\selfcheck.js
```

预期结果：

```text
SELF_CHECK_OK
```

---

## 14. 浏览器验收

通过静态服务器运行：

```powershell
cd D:\xhs\guofeng-vibecoding-studio
python -m http.server 5173
```

在浏览器验证：

- 页面状态码为200；
- favicon、CSS、JS 均为200；
- Console 0 errors、0 warnings；
- 首次打开立即理解使用步骤；
- 九个心绪按钮均可选择；
- 自定义关键词可用；
- 四类文化意象可切换；
- 五言／七言可切换；
- 落款进入诗签；
- 保存得到1200×1600 PNG；
- 复制文案成功；
- 换一签成功；
- 同题挑战可复制；
- 历史可恢复和清空；
- 刷新页面后历史仍存在；
- 320px 宽度无横向滚动；
- 键盘可操作所有控件；
- 减弱动效模式可用。

---

## 15. 实施顺序

严格按以下顺序构建：

### P0：规则和生产清理

1. 删除隐藏 Gemini 面板和生产引用；
2. 修正参赛标签；
3. 将 Canvas 改成真正3:4；
4. 更新名称为五言／七言诗签；
5. 清理 README 和参赛文档。

### P1：核心质量

1. 重构数据结构；
2. 实现种子随机；
3. 实现起承转合模板；
4. 实现韵组与去重；
5. 实现校验与保底诗签；
6. 实现创作释义。

### P2：竞争力功能

1. 用户落款；
2. 三套视觉主题；
3. 最近诗签；
4. 同题挑战；
5. 移动端自动滚动与粘性操作。

### P3：交付

1. 更新发布文案；
2. 完善自检；
3. 浏览器验收；
4. 生成小红书上传包；
5. Git 提交。

---

## 16. 完成标准

只有同时满足以下条件才算完成：

- 用户可在三步内生成诗签；
- 诗签为四句且字数稳定；
- 关键词和主题对四句均有实际影响；
- 输出图片为真正3:4高清 PNG；
- 生产页面没有隐藏开发工具；
- 文案包含两项活动的准确标签；
- 作品具有保存、复玩和同题分享闭环；
- 基础功能完全本地运行；
- 自检全部通过；
- 浏览器 Console 为零错误、零警告；
- 已生成可直接上传的小红书生产压缩包；
- Git 工作区干净且存在最终提交。

---

## 17. 最终回复格式

构建完成后只汇报：

1. 已实现功能；
2. 修改和新增的文件；
3. 自检结果；
4. 浏览器验收结果；
5. 生产压缩包绝对路径；
6. Git 提交哈希；
7. 仍需用户在小红书后台完成的上传、挂载和发布步骤。

不要用“建议后续实现”代替本任务中的必做项。
